{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --https --ssl-cert localhost.pem --ssl-key localhost-key.pem",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/svg": "^0.4.0",
    "nuxt": "3.0.0-rc.8",
    "sass": "^1.54.8",
    "sass-loader": "^13.0.2"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^5.3.2",
    "@storyblok/nuxt": "^4.2.0",
    "hamburgers": "^1.2.1"
  }
}
